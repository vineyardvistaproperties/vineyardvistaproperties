const crypto = require('crypto');
function safeEqual(a,b){const A=Buffer.from(String(a||''));const B=Buffer.from(String(b||''));return A.length===B.length&&crypto.timingSafeEqual(A,B)}
function token(area,secret){return crypto.createHmac('sha256',secret).update('vvp:'+area+':authorized').digest('hex')}
module.exports=async function(req,res){if(req.method!=='POST'){res.status(405).json({error:'Method not allowed'});return;}const body=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{});const area=body.area==='management'?'management':'site';const expected=area==='management'?process.env.MANAGEMENT_PASSWORD:process.env.SITE_PASSWORD;const secret=process.env.AUTH_SECRET;if(!expected||!secret){res.status(500).json({error:'Site authentication is not configured.'});return;}if(!safeEqual(body.password,expected)){res.status(401).json({error:'Incorrect password.'});return;}const name=area==='management'?'vvp_management':'vvp_site';res.setHeader('Set-Cookie',`${name}=${token(area,secret)}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=43200`);res.status(200).json({ok:true});}