const crypto=require('crypto');
function cookies(req){return Object.fromEntries(String(req.headers.cookie||'').split(';').map(v=>v.trim()).filter(Boolean).map(v=>{const i=v.indexOf('=');return [decodeURIComponent(v.slice(0,i)),decodeURIComponent(v.slice(i+1))]}));}
function token(secret){return crypto.createHmac('sha256',secret).update('vvp:site:authorized').digest('hex')}
function ok(a,b){if(!a||!b)return false;const A=Buffer.from(a),B=Buffer.from(b);return A.length===B.length&&crypto.timingSafeEqual(A,B)}
module.exports=async function(req,res){const secret=process.env.AUTH_SECRET;if(!secret||!ok(cookies(req).vvp_site,token(secret))){res.status(401).json({error:'Authentication required.'});return;}if(req.method!=='POST'){res.status(405).json({error:'Method not allowed'});return;}const b=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{});const required=['firstName','lastName','email','arrival','departure','adults'];if(required.some(k=>!String(b[k]||'').trim())){res.status(400).json({error:'Please complete all required fields.'});return;}const reference='VVP-'+Date.now().toString(36).toUpperCase();console.log('Vineyard Vista stay request',reference,b);res.status(202).json({ok:true,reference,message:`Request received. Reference ${reference}. This demo does not yet send confirmation email.`});}